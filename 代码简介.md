# MWLC 赛事协助系统 — 代码简介

## 一句话说明

这是一个给 **MWLC 赛事组** 用的赛事管理网站：游客可以看规则和选手资料，裁判/录像可以上传比赛结果和视频链接，管理员可以排日程、管名单、管成员、传资料、改规则、看结果总览。

## 它是怎么组成的

整个项目分两部分，放在两个文件夹里：

| 部分 | 目录 | 技术 | 干什么的 |
|------|------|------|---------|
| 后端 | `backend/` | Node.js + Express + better-sqlite3 + JWT + multer | 提供全部接口（登录、日程、结果、成员、名单、上传、设置），数据存在 SQLite 数据库 `backend/data/mwlc.db` |
| 前端 | `frontend/` | Vue 3 + Vite + Vue Router + Pinia + TipTap | 浏览器里看到的页面，通过 `/api` 调后端接口 |
| 上传文件 | `uploads/` | 本地磁盘目录 | 存放横幅、选手名单、地图、裁判截图等文件 |

启动方式（`./start.sh`）：后端跑在 **3001** 端口，前端开发服务器跑在 **5173** 端口，前端把 `/api` 和 `/uploads` 代理到后端，所以只访问 5173 一个地址就能用完整应用。

## 谁用什么（身份与权限）

登录账号是 **fanbookID**（一串数字），默认密码 `MWLC123456`。

| 身份 | 权限级别 | 能干什么 |
|------|---------|---------|
| 开发者 / 超级管理员 | superadmin | 全部权限，还能管其他超管、看/改任何人的密码 |
| 主办 / 管理 / 裁判长 | admin | 全部管理功能（日程、成员、名单、上传、规则、结果总览），但不能改别人密码、不能动超管账号 |
| 裁判 / 录像 | official | 看日程，上传/修改自己负责场次的结果（比分、截图、备注），录像加视频链接（最多 7 局） |
| 赛事工作人员 | staff | 只能看日程 |
| 普通用户 / 游客 | guest | 不用登录，只能看规则页 |

预置账号：
- 主办：fanbookID `1000000`，密码 `MWLC123456`（超管，首次启动自动升级）
- 赛事综合组：fanbookID `20605142`，密码 `MWLC123456`（admin，可同时干管理/裁判/录像的活）

## 页面和功能（按用户视角）

### 1. 规则展示区（不需要登录）
- 首页 `/` 是**赛事选择**页，列出所有赛事，点卡片进入对应赛事的规则页
- 规则页 `/rules/:code` 展示：
  - 赛事名称、介绍、横幅、报名表按钮
  - 富文本规则正文（支持加粗、颜色、高亮、表格、链接、列表、标题等）
  - 左侧**自动目录**，点击跳转、滚动时高亮当前章节
  - 可调正文字号（记忆在浏览器里）
  - 选手名单文件下载链接
  - 自定义页面背景（颜色或图片）

### 2. 裁判工作台（登录后 `/dashboard`）
- 日程分「待完成 / 已完成」两个标签页
- 每张日程卡片显示：`赛事名 | 组别 | 轮次-序号 | 对阵`、房间号、地图、时间
- 点卡片进日程详情 `/schedule/:id`：
  - 完整信息：轮次、序号、对阵、房间、时间、双方队伍完整名单（P1-P7 或 T1/T2/替补）、地图、备注、标签
  - **上传/修改结果**：比分、胜者（可按比分自动推断）、裁判ID、录像ID、截图（最多 10 张、单张 ≤15MB）、备注；传完自动移入已完成
  - **录像加链接**：按局填视频链接，最多 7 局
  - 工作台提供「查询缺链接组别」一键列出所有没传链接的已完成场次
- 工作台上还能改自己的密码

### 3. 管理后台（admin `/admin`）
- **日程管理**：创建/编辑/删除日程；创建时自动打标签「创建于\<时间\>,来自于\<创建人\>」；可从选手名单一键选 A/B 队，自动带出 P1-P7 队员信息；房间号可自动生成
- **选手名单**：按队伍分组展示（P1=队长，P2 起为主力/替补，最多 P7）；单条增改删、批量删除；支持 CSV/Excel 批量导入，自动匹配列名、预览确认、统一指定缺失位置（自动归一化「1~7」「一~七」等写法）
- **成员管理**：按 fanbookID 加成员、分配身份（可多选）和角色、重置密码（只有超管能改别人密码）、删除、批量删除；支持 CSV/Excel 批量导入裁判/录像名单
- **数据上传**：按类型（横幅/选手名单/地图/文档/其他）上传文件；管理比赛地图名称（日程表单里可选）
- **规则编辑**：TipTap 富文本编辑器，编辑规则正文、字号、文字颜色、高亮、表格、链接、报名表按钮；配置页面背景、正文背景；上传/删除横幅
- **赛事设置**：改赛事名称（顶栏、标题即时生效）
- **结果总览**：把每场比赛生成「CLOSE」格式文本（对阵/时间/比分/胜者🏆晋级/裁判/录像/备注），一键复制；也能查缺链接组别、删除结果

### 4. 个人设置（登录后 `/settings`）
- 改姓名、换头像
- 三态主题切换：跟随系统 / 浅色 / 深色（记忆在浏览器）
- 修改自己的密码、退出登录

## 后端接口大致结构

- `POST /api/auth/login`、`/change-password`、`GET /me` —— 登录与个人信息
- `/api/schedules` —— 日程的增删改查、按状态/组别筛选、缺链接查询
- `/api/results` —— 结果的增删改查、加视频链接（multipart 上传截图）
- `/api/members` —— 成员增删改、角色列表、批量导入/删除
- `/api/players` —— 选手名单增删改、按队伍分组、批量导入
- `/api/uploads` —— 文件上传/列表/删除（静态文件通过 `/uploads/...` 直接访问）
- `/api/settings`、`/api/tournaments` —— 赛事设置与多赛事管理（规则内容、背景、地图、报名链接）
- 权限靠 JWT 中间件控制：`requireAuth`（登录）、`requireAdmin`（admin/超管）、`requireSuperAdmin`、`requireRules`（规则管理/管理/超管）

## 数据模型（数据库表）

- `users` 成员（fanbookID、密码、姓名、身份 title、角色 role）
- `schedules` 日程（组别、轮次、序号、对阵、房间、时间、双方队伍与名单 lineup、地图、备注、标签、所属赛事）
- `results` 比赛结果（比分、胜者、裁判/录像ID、截图列表、视频链接列表、备注，与日程一一对应）
- `players` 选手名单（队伍、姓名、fanbook、游戏ID、位置 P1-P7、所属赛事）
- `uploads` 上传文件记录（类型、原文件名、路径、上传人、所属赛事）
- `tournaments` 赛事（代码、名称、介绍、规则内容、背景、地图、报名链接）
- `settings` 键值配置（兼容旧数据）

## 本次代码审查修复的问题

1. **日程详情里截图/链接显示错乱（重要）**：后端返回日程时，`result.screenshots` 和 `result.game_links` 直接透传了数据库里的 JSON 字符串，前端却当数组用，导致：视频链接表单被拆成一串乱字符、截图显示为乱字符、工作台徽标永远显示「已传」。
   → 修复：后端 `formatSchedule` 把这两个字段解析成数组后再返回（`backend/src/routes/scheduleRoutes.js`）。
2. **成员身份无法清空**：编辑成员时把身份全部取消勾选（传空数组），后端判断「没传」而不更新，旧身份一直残留。
   → 修复：只要显式传了 `titles` 数组（含空数组）就更新（`backend/src/routes/memberRoutes.js`）。
3. **删除上传文件后磁盘文件残留**：删除只删数据库记录，物理文件一直留在 `uploads/` 目录里，越积越多。
   → 修复：删除时同步清理磁盘文件（`backend/src/routes/uploadRoutes.js`）。
4. **删除比赛结果后截图残留**：同样的残留问题；另外用「替换截图」模式时旧截图也不清理。
   → 修复：删除结果、替换截图时清理对应截图文件（`backend/src/routes/resultRoutes.js`）。
5. **创建日程时标签与备注互相污染**：备注里只要含「创建于」三个字，整段备注就会被当成标签写进 tags，同时详情页会隐藏该备注。
   → 修复：创建标签固定写进 `tags` 字段，与用户备注互不干扰（`backend/src/routes/scheduleRoutes.js` + `frontend/src/views/ScheduleDetailView.vue`）。

修复后已通过 18 项接口回归测试（创建日程/结果、字段类型、缺链接查询、文件上传删除、成员身份清空、数据清理等），前端也重新构建通过。
